"""RAG indexer stub (filled in Phase 3.3)."""

from __future__ import annotations

import uuid


async def index_document(_document_id: uuid.UUID) -> int:
    """Returns the number of chunks indexed. Phase 3.3 implements this."""
    return 0
