"""Synthetic data generation + DB seeding.

`seeder` orchestrates loading the committed JSON / markdown files into
Postgres via the repositories. `narrative` and the prompt templates
define the 5-day storyline used by the message generator.
"""
