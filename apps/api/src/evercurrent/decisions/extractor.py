"""Decision extractor stub (filled in Phase 7)."""

from __future__ import annotations

import uuid


async def extract_decisions_for_day(_project_id: uuid.UUID, _day: int) -> int:
    """Returns the number of decisions extracted. Phase 7 implements this."""
    return 0
